package com.example.roomlesson

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.view.View
import android.widget.EditText
import android.widget.TextView
import com.example.roomlesson.WordListAdapter.Companion.PIZZY

class editingPage : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_editing_page)
       /* val message = intent.getStringExtra(PIZZY)
        val viewTheText = findViewById<EditText>(R.id.textView2).apply {
            text = message*/
        }
    }

